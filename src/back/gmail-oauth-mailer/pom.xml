<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.servify</groupId>
    <artifactId>gmail-oauth-mailer</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>
    <name>Gmail OAuth Mailer</name>
    <description>Envio de e-mail via Gmail API (OAuth2), sem SMTP.</description>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- Alinhar com a BOM de clientes Google quando possível -->
        <google.api.client.version>2.7.2</google.api.client.version>
        <google.oauth.jetty.version>1.39.0</google.oauth.jetty.version>
        <google.apis.gmail.version>v1-rev20251110-2.0.0</google.apis.gmail.version>
        <angus.mail.version>2.0.4</angus.mail.version>
    </properties>

    <dependencies>
        <!-- Cliente HTTP e JSON usados pela Gmail API -->
        <dependency>
            <groupId>com.google.api-client</groupId>
            <artifactId>google-api-client</artifactId>
            <version>${google.api.client.version}</version>
        </dependency>

        <!-- Gmail API oficial (REST); o envio usa users.messages.send, não SMTP -->
        <dependency>
            <groupId>com.google.apis</groupId>
            <artifactId>google-api-services-gmail</artifactId>
            <version>${google.apis.gmail.version}</version>
        </dependency>

        <!-- Fluxo OAuth2 com receiver local (navegador + callback em localhost) -->
        <dependency>
            <groupId>com.google.oauth-client</groupId>
            <artifactId>google-oauth-client-jetty</artifactId>
            <version>${google.oauth.jetty.version}</version>
        </dependency>

        <!-- API Jakarta Mail (MimeMessage, multipart, etc.) -->
        <dependency>
            <groupId>jakarta.mail</groupId>
            <artifactId>jakarta.mail-api</artifactId>
            <version>2.1.3</version>
        </dependency>

        <!-- Implementação Jakarta Mail (necessária em runtime para construir MimeMessage) -->
        <dependency>
            <groupId>org.eclipse.angus</groupId>
            <artifactId>angus-mail</artifactId>
            <version>${angus.mail.version}</version>
            <scope>runtime</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.5.0</version>
                <configuration>
                    <mainClass>com.servify.gmail.oauth.Main</mainClass>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.2</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>com.servify.gmail.oauth.Main</mainClass>
                            <addClasspath>true</addClasspath>
                            <classpathPrefix>lib/</classpathPrefix>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
